{
  "name": "traffic-controller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Air-traffic-control style tracker for parallel Claude sessions (Phase 1: watcher spike)",
  "scripts": {
    "dev": "tsx watch src/watcher.ts",
    "start": "tsx src/watcher.ts",
    "once": "tsx src/watcher.ts --once",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chokidar": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
